
document.addEventListener('DOMContentLoaded', () => {
  const cal = document.querySelector('.cal-sugerido, .calendar, #calendario, #calendar');
  if(!cal) return;
  cal.classList.add('calendar','cal-sugerido');
});
